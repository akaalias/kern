# Testing Initiative — Progress Log

Living status tracker so the initiative can be stopped and resumed at any point. **Update this file as work lands** (and append a dated session-log entry each time).

> Companion docs: [TESTING_PLAN.md](./TESTING_PLAN.md) (the strategy) · [REFACTORING.md](./REFACTORING.md) (candidates the net unlocks)

**Legend:** `[ ]` pending · `[~]` in progress · `[x]` done · `[-]` dropped/deferred

## Decisions locked (2026-06-24)
- Feature tests: **headless-first + thin GUI smoke** (real code, offscreen capture renderer; no pixel automation for the bulk).
- Controller seam: **extract early, after the pure-core net exists**, guarded by characterization snapshots.
- Build/CI: **portable Make/CMake headless build + GitHub Actions** (Linux for headless, macOS for app build + smoke).
- Framework: **single-header minimal harness** + ASan/UBSan/LSan + coverage.

## Phase checklist

**Phase A — Foundation + pure-core unit tests**
- [x] `tests/Makefile`: headless build of buffer/editing/undo (+ navigation/md_render/recent/commands) + SDL headers only, under ASan+UBSan (`SDL_CFLAGS` overridable for Linux libsdl2-dev). Geometry types come from `gfx.h` (microui is gone).
- [x] `tests/test.h` single-header harness + `tests/test_main.c` runner
- [x] `tests/unit_editing.c` (ed_insert/backspace/enter incl. list continuation + indent/outdent) — 12 tests green, 0 leaks
- [x] `tests/unit_undo.c` (insert/backspace/enter/list-group/indent undo, coalesced-run, empty-stack no-op) — 7 tests; fixtures factored into `tests/ed_fixture.h`
- [x] `tests/unit_buffer.c` — load, CRLF strip, missing-file, overlong-line split, save/load byte round-trip, line-array growth past cap, region ordering, **plus** `buf_resolve_path` (incl. `~`/leading-space/untitled), filename completion + subfolder + list-matches, `buf_save_text`, empty-file load, sandbox-dir getter, and error guards.
- [x] Invariant + characterization tests — undo round-trips, multi-line kill/replace, snapshot goldens (parity proof)
- [x] GitHub Actions Linux job (`.github/workflows/ci.yml`, clang + ASan/UBSan/LSan) — pending first remote run

**Phase B — Stub renderer + layout/integration + snapshots**
- [x] `tests/stub_renderer.{c,h}` — capture impl of `renderer.h` (deterministic 10×20 glyphs, 800×600 window → 700px page; records draw ops; stubs `SDL_GetTicks`). Build links `navigation.c` + `md_render.c` (no microui).
- [x] `tests/unit_navigation.c` (6 tests) — wrap breaks: empty, short, exact-page-width, mid-word break, last-space break, `nav_count_wraps` caching
- [x] `tests/unit_md_render.c` (6 tests) — **multi-row bold carry-over** (guards the wrapped-span fix), markers use base style, full bold line, `==highlight==` bg-per-glyph, wikilink bg, linear `md_col_x`
- [x] Snapshot harness (`tests/snapshot.c`) + `tests/snapshots/` goldens — renders representative docs (plain/inline/inline-wrap/lists/headings) through the real wrap + md_draw_text path into the stub's ordered op stream, serializes a deterministic model, and diffs vs committed goldens. Regenerate with `KERN_UPDATE_SNAPSHOTS=1 make test`. **This is the parity proof for future refactors.**
- [x] More integration breadth: `nav_click_to_cursor` (incl. wrapped lines), cursor-visibility/clamp, cross-line word motion, list-metric + heading-prefix predicates

**Phase C — Controller seam extraction (behavior-preserving)**
Approach: extract-then-test in small slices — pull self-contained pieces out of `textview.c` into modules compiled into BOTH the app and the headless test binary, growing automated coverage over `textview.c` before the riskier dispatch/de-globalization surgery. (The snapshot/unit nets cover the render+core path, NOT `textview.c`, so each slice is verified by app-build + run + new unit tests.)
- [x] Decompose slice 1: recent-files MRU → `Editor/recent.{c,h}` + `tests/unit_recent.c` (5 tests). App builds + runs; render snapshots unchanged.
- [x] Decompose slice 2: clipboard seam → `Editor/clipboard.h` + `Editor/clipboard_sdl.c` (app); the copy/yank commands now call `kern_clipboard_*` instead of SDL directly. Added 6 kill-ring unit tests (`ed_emacs_kill_line`/`copy_region`/`yank`, incl. multi-line + copy→yank round-trip). A test-side fake clipboard will be added when the dispatch is extracted.
- [x] Decompose slice 3: clock seam → `Editor/clock.h` + `Editor/clock_sdl.c` (app); `navigation.c` status timing + `textview.c` autosave now call `kern_now_ms()`. Test build links `tests/clock_fake.c` (settable) and the `SDL_GetTicks` stub is retired. Added 2 `nav_status` tests (message expiry + mode-indicator priority) via the fake clock.
- [x] Extract `kern_dispatch_key` + de-globalize the command table, in batches → `Editor/commands.{c,h}` (explicit `EditorState*`/`ViewState*`); each batch feature-tested in `tests/unit_commands.c`. **The duplicate `KeyBinding`/`normal_bindings`/`check_binding` table in `textview.c` has since been deleted — `kern_dispatch_key` is the only table.**
  - [x] Batch 1: cursor movement (C-a/C-e/C-f/C-b/C-n/C-p, M-f/M-b) — 7 tests.
  - [x] Batch 2a: table-only editing (C-d, Backspace, Return, C-t, C-o, C-/, C-Space, C-g) — 7 tests.
  - [x] Batch 2b: kill/yank/copy/case (C-k, C-w, C-y, M-w, M-d, M-DEL, M-u/M-l/M-c) — added `tests/clipboard_fake.c`; exposed the 5 ESC/meta-shared commands in `commands.h` and updated those call sites. 7 tests (incl. copy→yank via clipboard). The `clipboard_set_from_kill` helper moved into `commands.c`.
  - [x] Batch 3: page/recenter/font (C-v, M-v, C-l, Cmd-=/Cmd--) + buffer-ends (M-S-,/. and Cmd-S-,/.) + mark (C-x h, C-x C-x). 5 tests. **The legacy table is now just `M-g`.**
  - [x] `cmd_goto_line` (M-g) — stays in `textview.c` (it drives the minibuffer statics), but is now dispatched by a single inline check after `kern_dispatch_key`, not a second table.
- [x] Extract `editor_handle_event` / `editor_tick` from `editor_main`'s switch + slim it to a bootstrap — **done, and turned into the test seam.** `editor_main`/`resize_event_watcher` are `#ifdef`'d out under `KERN_HEADLESS_TEST`; `Editor/editor_loop.h` declares the extracted functions + the `tv_test_*` accessor/reset seam. `tests/unit_textview.c` (24 tests) pumps synthetic `SDL_Event`s through `editor_handle_event`, covering prefix chords, modal-routing precedence, wikilink nav + dropdown, text-input gating, arrows, isearch, autosave (via the fake clock), and minibuffer editing. `tests/platform_stub.c` fakes the SDL input-state calls + the `kern_x_*` bridge. Loop shape preserved (wait → drain → tick), so snappiness is unchanged; app builds + launches.

**Command de-globalization is complete:** all dispatch-table commands run on explicit `EditorState*`/`ViewState*` via `kern_dispatch_key` and are feature-tested in `unit_commands.c`. The `textview.c` shim/alias block and its duplicate binding table are gone; the arrow keys now delegate to the `commands.c` movement functions. Only `M-g` (minibuffer-bound) stays in `textview.c`, dispatched inline.

**Phase D — Feature / UX headless tests** (unblocked once commands are de-globalized; grows per Phase C batch)
- [x] Keybinding feature tests via `kern_dispatch_key` — full command set (movement, kill/yank/copy/case, page/recenter/font, buffer-ends/mark) feature-tested in `unit_commands.c`, including clamp/boundary branches.
- [~] Prefix chords (`C-x …`, ESC/meta), `Cmd-Shift-,/.` — still routed through `textview.c`'s `handle_*_key`, which the headless suite doesn't compile. Needs the keydown seam (see Next action).
- [~] Scenarios: editing/search/region/font are covered headlessly; wikilink follow+autocomplete+nav still live only in `textview.c` (untested) pending the seam.

**Phase E — Performance harness + corpora**
- [x] `tools/gen_prose.py` — deterministic Markdown-ish corpus generator (`<size> <out>`, e.g. `100MB`).
- [x] `tests/perf/perf_main.c` + `make perf CORPUS=…` — times load, cold/warm wrap-all, jump-to-end/top, search, full re-wrap, via the deterministic stub renderer (`-O2`, no sanitizers). `--check` enforces generous budgets. CI `perf-smoke` job runs `--check` on a 25MB corpus.
- [~] Guarded optimizations / refactors consuming the net:
  - **REFACTORING #1 (remove microui) — DONE.** microui was only used as a draw-command buffer + clip stack + geometry types + a white-pixel atlas; the editor draws immediately. Step 1: converted `textview.c`'s render + input to immediate-mode `r_draw_rect`/`r_set_clip_rect` + SDL mouse tracking (verified pixel-identical via before/after screenshots). Step 2: replaced the geometry types/ctors with a ~30-line `Editor/gfx.h`, swapped `r_draw_rect` to sample a 1×1 white texel (provably equiv to the atlas white pixel), and **deleted microui.c / microui.h / atlas.inl (~3,000 lines)**. App builds + runs, snapshots byte-identical, 0 leaks, headless suite no longer links microui.
  - **REFACTORING #2/#9 (de-shim `textview.c` + collapse dispatch) — DONE.** Deleted the ~95-line `#define` alias block (old global names → `g_ed.`/`g_vs.` and the `buf_*`/`nav_*`/`ed_*`/`md_*` APIs); removed the duplicate `KeyBinding`/`normal_bindings`/`check_binding` table (a clone of `Command`/`g_commands`/`cmd_matches` holding one entry); arrow keys now delegate to the `commands.c` movement functions. App builds; suite green. Net −137 lines. (Prefix-chord unification — the rest of #9 — is still open.)
  - **REFACTORING #4 (md_render span-map cache):** inline spans are now computed once per line and cached on `Line` (lazy, invalidated by `line_dirty`, freed in the line lifecycle), instead of re-parsed for every wrapped visual row. `md_draw_text`/`md_col_x` now take a `Line *` and consume the cache. **Snapshots byte-identical** (parity proven), 0 leaks, md_render coverage 91%. Removes the latent O(rows×len) re-parse; clarity win (parse separated from draw).
  - **first refactor (REFACTORING #6, parity-safe half):** `nav_maybe_reflow` skips the full re-wrap when a resize doesn't change the page width (height-only / spurious resizes). Perf: **resize same-width 377ms → 0.8ms** (~480×); genuine width-change unchanged. Guarded by a unit test + perf scenario + snapshots (correctness identical). The per-word-metrics half of #6 is **deferred**: the renderer rounds width per-character, so per-word measurement would shift wrap points in the real app — and the kerning/rounding-free stub can't catch that, so the net can't guard it.

**Baseline (2026-06-24, 100MB / 1.32M lines, Apple silicon, -O2):** load 106ms · wrap-all cold 523ms · wrap-all warm 1.7ms · jump-to-end 1.6ms · re-wrap(resize) 510ms · search ~0ms. The two ~0.5s costs (cold wrap, full re-wrap on resize) are the `invalidate_all_wraps` hotspot (REFACTORING #6).

**Phase F — GUI smoke + CI matrix**
- [ ] Handful of end-to-end smoke tests (SDL_PushEvent / XCUITest)
- [ ] CI matrix: Linux headless (+coverage +short fuzz) and macOS (Xcode build + smoke)

## Cross-cutting (land alongside the phases)
- [x] Sanitizers wired into every headless build (A) — ASan+UBSan in `tests/Makefile`; LSan auto on Linux CI
- [x] Coverage (llvm-cov) — `make coverage` reports line/fn/region/branch for the Editor core; CI `coverage` job enforces a **≥85% line floor**. Currently **99.46% lines / 100% functions / 85.6% branches / 95.8% regions** (lines: buffer 100, editing 100, undo 96, navigation 99.6, md_render 99.5, recent 100, commands 100). The 8 remaining uncovered lines are unreachable by construction (closing braces after a `switch` `break`, a dead `default`, a defensive `return`, a malloc-failure path); the remaining ~125 uncovered branches are malloc/IO-failure sides (need fault injection) and defensive guards.
- [ ] libFuzzer targets: `buf_load_file`, `md_detect_span` (E/F)
- [ ] Differential-test harness for hot-function rewrites (C/E)

## Session log
- **2026-06-24** — Architecture mapped; strategy decided (4 choices above); plan + this tracker written and promoted from `~/.claude/plans/` into `docs/`. Refactoring-candidates doc created (`REFACTORING.md`).
- **2026-06-24** — **Phase A first slice landed.** `tests/` headless build (Makefile + test.h + test_main.c) compiling buffer/editing/undo under ASan+UBSan; `unit_editing.c` with 12 tests (insert/backspace/enter/list-continuation/indent/outdent) — all green, 0 leaks (verified via macOS `leaks`). Linux CI workflow added. **No app code changed.** App source untouched; only new `tests/`, `.github/`, `.gitignore` guard.
- **2026-06-24** — Added `tests/unit_undo.c` (7 tests) and factored shared fixtures into `tests/ed_fixture.h`. Suite now **19 tests / 47 checks**, all green, 0 leaks. App code still untouched.
- **2026-06-24** — Added `tests/unit_buffer.c` (7 tests: load, CRLF strip, missing-file, overlong split, save/load byte round-trip, line-array growth, region ordering). **Pure-core unit layer (buffer/editing/undo) now covered: 26 tests / 81 checks, green, 0 leaks.** App code still untouched.
- **2026-06-24** — **Phase B started.** Added `tests/stub_renderer.{c,h}` (capture renderer, no GL) and wired `navigation.c` + `md_render.c` + `microui.c` into the headless build. New `unit_navigation.c` (6) + `unit_md_render.c` (6). Suite **38 tests / 109 checks, green, 0 leaks.** Multi-row bold test guards the earlier wrapped-span fix.
- **2026-06-24** — Fixed Linux CI build (`-D_GNU_SOURCE` exposes POSIX `mkstemp` that glibc hides under `-std=c11`; macOS built fine without it).
- **2026-06-24** — **Snapshot harness landed** (`tests/snapshot.c` + goldens in `tests/snapshots/`): rendered through the real wrap+md_draw_text path → deterministic serialized model diffed against committed goldens. Broadened to 7 scenarios covering every inline feature — plain, inline (bold/italic/code/wikilink/highlight), inline-wrap, lists, headings (#/##/###), **links (`[text](url)`)**, and **edges** (unterminated/empty/non-nesting delimiters). Suite now **45 tests / 116 checks, green, 0 leaks.** App code still untouched.

- **2026-06-24** — **Phase C started (first app-code change).** Extracted the recent-files MRU out of `textview.c` into `Editor/recent.{c,h}` (with `recent_count()`/`recent_get()`/`recent_reset()` accessors), wired into both the app (auto via folder-sync) and the headless build. `tests/unit_recent.c` (5 tests). App builds + launches; render snapshots unchanged. (`C-x b` confirmed working by user.) Suite **50 tests / 132 checks.**
- **2026-06-24** — **Phase C slice 2: clipboard seam.** `Editor/clipboard.h` + `Editor/clipboard_sdl.c`; copy/yank now go through `kern_clipboard_*` instead of touching SDL. Added 6 kill-ring unit tests. (Copy/yank confirmed working by user.) Suite **56 tests / 147 checks.**
- **2026-06-24** — **Phase C slice 3: clock seam.** `Editor/clock.h` + `Editor/clock_sdl.c`; `navigation.c`/`textview.c` time now via `kern_now_ms()`. Test build uses a settable `clock_fake.c`; retired the `SDL_GetTicks` stub. Added 2 `nav_status` tests (expiry + priority). App builds + launches. Suite **58 tests / 155 checks, green, 0 leaks.**
- **2026-06-24** — **Command de-globalization (batches 2a/2b/3) landed**: table-only editing, kill/yank/copy/case, page/recenter/font + buffer-ends/mark all migrated to `commands.c` and feature-tested via `kern_dispatch_key`; the legacy table shrank to just `M-g`.
- **2026-06-24** — **REFACTORING #1: microui removed.** Step 1 converted render + input to immediate mode; step 2 deleted `microui.c`/`microui.h`/`atlas.inl` (~3,000 lines) and added a ~30-line `gfx.h`. Snapshots byte-identical; headless suite no longer links microui. Later renamed `gfx.h` types to clean non-`mu_` names and removed the microui leftovers from the test build. Also: font-state-dependent wrap fix, `Cmd-Shift-N` extract-region-to-note.
- **2026-06-24** — **Coverage push 75% → 88% lines** (added `unit_search.c`, buffer path/completion tests, multi-line-undo); CI line floor set to ≥85%.
- **2026-06-25** — **REFACTORING #2/#9: de-shimmed `textview.c` + collapsed dispatch.** Deleted the `#define` alias block and the duplicate `KeyBinding` table; arrows delegate to `commands.c`. Net −137 lines; app builds, suite green.
- **2026-06-25** — **Coverage push 88% → 99.46% lines / 100% functions** (batches A–G: editing/undo/navigation/search/md_render/commands/buffer edge cases incl. the previously-0% `nav_click_to_cursor`). Suite **97 → 123 tests**.
- **2026-06-25** — **Branch-coverage push 79% → 85.6%** (reachable clamp/boundary/no-op/early-return branches across all suites). Suite **123 → 155 tests / 382 checks, green, 0 leaks.** Remaining uncovered branches are malloc/IO-failure sides + defensive guards (need fault injection).
- **2026-06-25** — **`textview.c` event/dispatch layer made headless-testable (the seam the prior "next action" called for).** Extracted `editor_handle_event`/`editor_tick` from `editor_main` (loop shape preserved); guarded `editor_main`/`resize_event_watcher` behind `KERN_HEADLESS_TEST`; added `Editor/editor_loop.h` (+ `tv_test_*` seam), `tests/platform_stub.{c,h}` (SDL input-state + `kern_x_*` fakes), and `tests/unit_textview.c` (**24 tests**) driving real `SDL_Event`s through the dispatcher — prefix chords / `ESC`-clears-mark-vs-meta, modal-routing precedence, wikilink follow+history+dropdown, text-input gating (modifier guard / `suppress_next_text` / literal-tab drop), arrow shift-select + word-jump + target column, isearch repeat/flip/abort, autosave timer (fake clock), and minibuffer find/save-as/Tab-completion. `textview.c` added to the test build (kept **out** of the gated `COVCORE` floor so its partly-tested shell doesn't mask the core's ~99% saturation; `make coverage-textview` reports it separately). Suite **~179 → 203 tests / 502 checks, green, 0 leaks**; `textview.c` coverage **0% → ~53% lines / 60% functions** (uncovered = `do_render`/`process_frame` GL chrome + `#ifdef`'d-out `editor_main`). App builds + launches (live pid confirmed).

- **2026-06-25** — **Caret-geometry bug fixes (click placement + vertical movement).** Click-to-cursor and `C-n`/`C-p`/arrows ignored the render's hanging list indent and per-span font widths, and moved by *logical* line not *visual* row. Fixed by mirroring `do_render`: added `md_row_indent` + `md_x_to_col` (`md_render.c`), routed `nav_get_wrap_breaks`/`nav_click_to_cursor`/new `nav_cursor_x`/`nav_visual_move` through them, and shared `nav_heading_markers_hang` (removed the `textview.c` duplicate). Vertical movement now keeps a pixel goal column (`ViewState.goal_x/goal_line/goal_col`). Added **5 `unit_navigation.c` tests** (list-indent click, visual-move-stays-in-wrapped-line, goal-x persistence, buffer-end clamps, wrap-respects-list-indent). `navigation.c` now links `md_render.c`+`pos_render.c`+`style_check.c`, so the **perf harness** (`tests/Makefile` `PERF_SRC`) links them + the fake tagger too. Suite **218 → 223 tests / 610 checks, green, 0 leaks**; core line coverage **96.72%** (≥85% floor); perf within all budgets. App builds + launches (live pid confirmed; user confirmed both click and arrow movement). Committed to `main`.

## Next action on resume
The `textview.c` event/dispatch frontier is now covered (`unit_textview.c`). What remains uncovered there is the **GL-render chrome** (`do_render`/`process_frame` — scrollbar drag, selection/search highlight layout, dropdown/buffer-switch *drawing*, typewriter pinning) and `editor_main` itself; these need either a thin GUI smoke harness (real offscreen SDL — see the `SDL_PushEvent`/XCUITest item) or render-assertion tests over the stub's captured op stream (the snapshot path) rather than more event tests. Secondary levers unchanged: a **fault-injection seam** (overridable `malloc`/`fopen`) for the remaining error-path branches, and REFACTORING #3 (split `textview.c`) / #9 (unify prefix chords) — now lower-risk since the dispatch layer is characterized by `unit_textview.c`.
