# Welcome to the Emacs-like Editor

This is a test of **markdown formatting** in our editor.

## Features

Here is some _italic text_ and some **bold text** together.

You can also use `inline code` for code snippets.

### Links

Here is a [link to something](https://example.com) and a [[wikilink]] too.

- First list item
- Second list item with **bold** in it
- Third item with `code` and _italics_

1. Numbered item one
2. Numbered item two
3. Third numbered item

## Code Examples

Use `r_draw_text()` to render text and `r_set_font_style(FONT_BOLD)` for bold.

### Mixed Formatting

This paragraph has **bold**, _italic_, `code`, and [links](url) all mixed together in flowing text that should wrap nicely across multiple lines while maintaining the correct formatting for each span.

---

# Another Heading

Some regular text after the heading. The **quick brown fox** jumps over the _lazy dog_. Check the `README.md` for more [[details]].
